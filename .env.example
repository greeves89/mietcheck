# =============================================================================
# MietCheck - Umgebungsvariablen
# Kopiere diese Datei zu .env:  cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Datenbank (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_USER=mietcheck
POSTGRES_PASSWORD=sicheres_passwort_hier_eintragen
POSTGRES_DB=mietcheck

# -----------------------------------------------------------------------------
# Sicherheit
# Zufälligen Schlüssel generieren mit: openssl rand -hex 32
# -----------------------------------------------------------------------------
SECRET_KEY=hier_openssl_rand_hex_32_ausgabe_eintragen
REFRESH_SECRET_KEY=hier_zweiten_openssl_rand_hex_32_eintragen

# -----------------------------------------------------------------------------
# SMTP E-Mail (optional)
# Ohne SMTP funktioniert die App, nur keine E-Mails werden versendet.
# Beispiel Gmail: smtp.gmail.com | Strato: smtp.strato.de | Port: 587
# -----------------------------------------------------------------------------
# Brevo (kostenlos bis 300 Mails/Tag): smtp-relay.brevo.com:587
# Eigener Anbieter: smtp.strato.de, smtp.gmail.com etc.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=noreply@deine-domain.de
SMTP_PASSWORD=dein_brevo_oder_smtp_passwort
SMTP_FROM=MietCheck <noreply@deine-domain.de>
SMTP_TLS=true

# -----------------------------------------------------------------------------
# URLs
# FRONTEND_URL: Öffentliche URL der App (für Links in E-Mails)
# NEXT_PUBLIC_API_URL: http://localhost/api wenn nginx auf Port 80
# -----------------------------------------------------------------------------
FRONTEND_URL=http://localhost
NEXT_PUBLIC_API_URL=http://localhost/api

# -----------------------------------------------------------------------------
# Umgebung
# -----------------------------------------------------------------------------
ENVIRONMENT=production

# -----------------------------------------------------------------------------
# Stripe Zahlungen
# Keys findest du unter: https://dashboard.stripe.com/apikeys
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Preis-ID für 0,99€/Monat - erstellen unter: https://dashboard.stripe.com/products
STRIPE_PRICE_ID=price_...
STRIPE_SUCCESS_URL=http://localhost/dashboard?upgraded=true
STRIPE_CANCEL_URL=http://localhost/settings?cancelled=true
